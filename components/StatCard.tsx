type StatCardProps = { label: string; value: string; delta: string; accent: string };
export function StatCard({ label, value, delta, accent }: StatCardProps) { return <div className="rounded-2xl border border-slate-800 bg-slate-900/70 p-5 shadow-soft"><div className="flex items-center justify-between"><p className="text-sm text-slate-400">{label}</p><span className={`rounded-full px-2 py-1 text-xs ${accent}`}>{delta}</span></div><p className="mt-4 text-3xl font-bold text-white">{value}</p></div>; }
