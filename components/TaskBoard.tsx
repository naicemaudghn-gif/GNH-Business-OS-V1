type Task = { id: number; title: string; project: string; status: string; priority: string; assignee: string; dueDate: string };
export function TaskBoard({ tasks }: { tasks: Task[] }) { const columns = ["À faire", "En cours", "Terminé"].map((key) => ({ key, items: tasks.filter((task) => task.status === key) })); return <div className="rounded-2xl border border-slate-800 bg-slate-900/70 p-5 shadow-soft"><div className="mb-5 flex items-center justify-between"><h2 className="text-xl font-semibold text-white">Kanban</h2><span className="text-sm text-slate-400">Vue tâches</span></div><div className="grid gap-4 md:grid-cols-3">{columns.map((column) => <div key={column.key} className="rounded-2xl border border-slate-800 bg-slate-950/60 p-3"><div className="mb-3 flex items-center justify-between"><h3 className="font-medium text-slate-200">{column.key}</h3><span className="rounded-full bg-slate-800 px-2 py-1 text-[10px] text-slate-300">{column.items.length}</span></div><div className="space-y-3">{column.items.map((task) => <div key={task.id} className="rounded-xl border border-slate-700 bg-slate-900 p-3"><div className="mb-2 flex justify-between gap-2"><span className="text-xs text-violet-300">{task.project}</span><span className="rounded-full bg-amber-500/15 px-2 py-1 text-[10px] text-amber-300">{task.priority}</span></div><p className="text-sm font-medium text-white">{task.title}</p><div className="mt-3 text-xs text-slate-400"><p>Assigné à: {task.assignee}</p><p>Échéance: {task.dueDate}</p></div></div>)}</div></div>)}</div></div>; }
