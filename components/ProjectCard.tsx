type Project = { id: number; name: string; description: string; status: string; tasks: number; progress: number };
export function ProjectCard({ project }: { project: Project }) { return <div className="rounded-2xl border border-slate-800 bg-slate-950/60 p-4"><div className="mb-4 flex items-center justify-between"><h3 className="text-lg font-semibold text-white">{project.name}</h3><span className="rounded-full bg-emerald-500/15 px-2 py-1 text-[10px] uppercase text-emerald-300">{project.status}</span></div><p className="text-sm leading-6 text-slate-400">{project.description}</p><div className="mt-4"><div className="mb-2 flex justify-between text-xs text-slate-400"><span>Progression</span><span>{project.progress}%</span></div><div className="h-2 rounded-full bg-slate-800"><div className="h-2 rounded-full bg-violet-500" style={{ width: `${project.progress}%` }} /></div></div><div className="mt-4 flex items-center justify-between text-sm text-slate-400"><span>{project.tasks} tâches</span><button className="rounded-lg border border-slate-700 px-3 py-1.5 text-violet-200 hover:border-violet-500">Ouvrir</button></div></div>; }
